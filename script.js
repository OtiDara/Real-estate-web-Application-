


//const carousel = document.querySelector('.carousel');
//const images = carousel.querySelectorAll('.carousel-card'); // Select each image card in the carousel
//let currentIndex = 0;
//const totalImages = images.length;

// Function to slide images automatically
//function autoSlide() {
    // Move to the next image
    //currentIndex++;
    
    // If we reach the end, reset to the first image
    //if (currentIndex >= totalImages) {
        //currentIndex = 0;
   // }

    // Calculate the percentage to move based on the image width
    //const translateValue = -currentIndex * 10; 
   // carousel.style.transform = `translateX(${translateValue}%)`;
//}

// Set the interval to slide the images every 3 seconds
//let slideInterval = setInterval(autoSlide, 10);

// Optional: Pause the sliding on hover
//carousel.addEventListener('mouseover', () => {
   // clearInterval(slideInterval);
//});

// Resume the auto-sliding when the mouse leaves the carousel
//carousel.addEventListener('mouseout', () => {
    //slideInterval = setInterval(autoSlide, 1000);
//});